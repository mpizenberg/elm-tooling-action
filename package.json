{
  "name": "elm-tooling-action",
  "version": "2.1.0",
  "description": "Install Elm tools and cache $ELM_HOME",
  "main": "src/main.mjs",
  "author": "Matthieu Pizenberg",
  "license": "MIT",
  "dependencies": {
    "@actions/cache": "^6.1.0",
    "@actions/core": "^3.0.1",
    "elm-tooling": "^1.20.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.44.0"
  },
  "scripts": {
    "prepare": "npm run build-main && npm run build-post",
    "build-main": "ncc build --minify src/main.mjs -o dist --license licenses.txt && mv dist/index.mjs dist/main.mjs",
    "build-post": "ncc build --minify src/post.mjs -o dist && mv dist/index.mjs dist/post.mjs"
  }
}
